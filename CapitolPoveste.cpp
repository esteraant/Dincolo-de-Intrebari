#include "CapitolPoveste.h"
/*
CapitolPoveste::CapitolPoveste(const std::string& titlu, const std::string& continut) : deblocat{false} // construtor de init
{
    this->titlu = titlu;
    this->continut = continut;
}

// Constructor default și destructorul sunt default, nu trebuie reimplementate aici

void CapitolPoveste::deblocheaza() {
    this->deblocat = true;
}

bool CapitolPoveste::esteDeblocat() const { // getter pt statusul deblocat
    return deblocat;
}

const std::string& CapitolPoveste::getTitlu() const { // getter pt titlu
    return titlu;
}

// operator<<
std::ostream& operator<<(std::ostream& os, const CapitolPoveste& cap) {
    os << "~     " << cap.titlu << "     ~\n";
    if (cap.deblocat) {
        os << cap.continut << "\n";
    }
    else {
        os << "Capitol blocat. Raspunde corect la intrebari pentru a-l debloca.\n";
    }
    return os;
}*/
